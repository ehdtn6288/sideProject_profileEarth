header.header
    .header__column
        a(href=routes.home)
            i.fas.fa-volleyball-ball  #{siteName}
            
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="검색", name = "term")
    .header__column
    ul
        li
            a(href=routes.join) Join
        li
            a(href=routes.login) Login
        li
            a(href=routes.upload) Upload